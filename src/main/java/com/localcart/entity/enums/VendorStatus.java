package com.localcart.entity.enums;

public enum VendorStatus {
    PENDING,    // Vendor application submitted
    APPROVED,   // Vendor approved and active
    SUSPENDED,  // Vendor suspended
    REJECTED    // Vendor application rejected
}
